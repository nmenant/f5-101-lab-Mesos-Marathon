tickTime=2000
dataDir=/var/lib/zookeeper/
clientPort={{ zookeeper_client_port }}
initLimit=10
syncLimit=5
{% for host in groups['masters'] %}
server.{{ hostvars[host].zoo_myid }}={{ host }}:{{ zookeeper_leader_port }}:{{ zookeeper_election_port }}
{% endfor %}